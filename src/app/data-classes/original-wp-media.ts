export interface OriginalWpMedia {
  alt_text: string;
  author: string;
  caption: {rendered: string};
  date: string;
  description: string;
  guid: {rendered: string};
  id: number;
  mimetype: string;
  post: number;
  title: {rendered: string};
}
